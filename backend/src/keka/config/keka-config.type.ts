export type KekaConfig = {
  mockEnabled: boolean;
  authUrl: string;
  apiBaseUrl: string;
  clientId: string;
  clientSecret: string;
  apiKey: string;
  companySubdomain: string;
  syncCron: string;
  syncEnabled: boolean;
};
