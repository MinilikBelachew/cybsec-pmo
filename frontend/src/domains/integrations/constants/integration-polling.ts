/** Background refresh for integration sync tables (ms). */
export const INTEGRATION_POLLING_INTERVAL_MS = 30_000;

/** Poll interval while a manually triggered Keka sync job is in flight. */
export const KEKA_SYNC_JOB_POLLING_INTERVAL_MS = 1_500;
